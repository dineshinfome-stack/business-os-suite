# Templates

Populated in Phase 4. Prompt templates land in Phase 3 under `prompts/`.

Do not duplicate templates that already exist in `docs/15-governance/` or `docs/99-templates/`. Reference the existing asset; extend only when a real gap is identified and logged in the Engineering Review.
