/**
 * Board Rec 8 — reserved slot for the future AI Assistant header entry.
 * Renders nothing today so the layout stays stable when it lands.
 */
export function AiAssistantSlot() {
  return null;
}
