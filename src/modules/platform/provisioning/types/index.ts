/**
 * Gate 3.4 · Provisioning DTO entry point.
 *
 * `v1` is the pinned namespace for external/mobile consumers; the flat
 * re-export is the convenience surface for this dashboard.
 */
export * as v1 from "./v1";
export * from "./v1";
