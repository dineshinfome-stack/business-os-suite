/**
 * Gate 3.8 · Pass 3.8.1 — DTO entry point.
 *
 * `v1` is the pinned namespace for external/mobile consumers; the flat
 * re-export is the convenience surface for application code.
 */
export * as v1 from "./v1";
export * from "./v1";
